{
  "name": "docs-generator",
  "version": "0.0.3",
  "main": "src/index.ts",
  "scripts": {
    "start:dev": "tsc --noEmit && ts-node-dev --transpile-only --respawn --project tsconfig.json src/index.ts",
    "start": "tsc --noEmit && node --import=../../register.js src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "dependencies": {
    "@types/semver": "^7.5.8",
    "axios": "^1.7.7",
    "fs-extra": "^11.2.0",
    "json-schema": "^0.4.0",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0"
  }
}
